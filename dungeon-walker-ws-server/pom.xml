<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.0</version>
    </parent>

    <groupId>momomomo.dungeonwalker</groupId>
    <artifactId>dungeon-walker-ws-server</artifactId>
    <name>Dungeon Walker WebSocket Server Parent</name>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>

    <properties>
        <java.version>25</java.version>
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <dungeon-walker-commons.version>0.0.1-SNAPSHOT</dungeon-walker-commons.version>
        <dungeon-walker-commons-spring.version>0.0.1-SNAPSHOT</dungeon-walker-commons-spring.version>
        <dungeon-walker-contracts.version>1.0-SNAPSHOT</dungeon-walker-contracts.version>
        <pekko.version>1.4.0</pekko.version>
        <apache-commons-lang.version>3.19.0</apache-commons-lang.version>
        <apache-commons-collections.version>4.5.0</apache-commons-collections.version>
        <jib.version>3.4.4</jib.version>

        <testcontainers.version>1.21.3</testcontainers.version>
    </properties>

    <modules>
        <module>wsserver-domain</module>
        <module>wsserver-core</module>
        <module>wsserver-transport</module>
        <module>wsserver-startup</module>
    </modules>

    <dependencies>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <plugin>
                <groupId>com.google.cloud.tools</groupId>
                <artifactId>jib-maven-plugin</artifactId>
                <version>${jib.version}</version>
                <configuration>
                    <from>
                        <image>eclipse-temurin:25-jre</image>
                    </from>
                    <to>
                        <image>alejoceballos/dungeon-walker-ws-server:v1</image>
                    </to>
                    <container>
                        <mainClass>momomomo.dungeonwalker.wsserver.startup.DungeonWalkerWsServer</mainClass>
                        <ports>
                            <port>8080</port>
                        </ports>
                    </container>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
