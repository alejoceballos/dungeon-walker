spring:
  application:
    name: dungeon-walker-ws-server

server:
  port: 8080

logging:
  level:
    root: info
    momomomo:
      dungeonwalker:
        wsserver:
          core: debug
          transport: debug
